{% for node in pages_list %}
    {% if group == null or group == node.group %}
   		<li >
        {% if page.url == node.url %}
			<a href="{{ BASE_PATH }}{{node.url}}" class="active btn btn-primary icon-bookmark" class="active">
				{{node.title}} - {{node.date_string}}
			</a>
    	{% else %}
			<a href="{{ BASE_PATH }}{{node.url}}" class="active btn btn-primary icon-bookmark">
				{{node.title}} - {{node.date_string}}
			</a>
        {% endif %}
   		</li>
    {% endif %}
{% endfor %}